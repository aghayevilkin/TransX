@model List<IndustriesServed>
@{
    ViewData["Title"] = "Industries Served";
}

<div class="content-wrapper">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Industries Served</h4>
            <div class="mr-2">
                <a asp-controller="service" asp-action="createindustriesserved" class="btn btn-info"><i class="mdi mdi-plus-circle-outline"></i> Add</a>
                <a asp-controller="service" asp-action="index" class="btn btn-danger"> Services</a>
            </div>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Name</th>
                            <th scope="col"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @{ var i = 1;}
                        @foreach (var item in Model)
                        {
                            <tr>
                                <th scope="row">@i</th>
                                <td>@item.Name</td>
                                <td>
                                    <a asp-area="admin" asp-controller="service" asp-action="updateindustriesserved" asp-route-id="@item.Id" class="btn btn-success">Update</a>
                                    <a onclick="return confirm('Are you sure?')" asp-area="admin" asp-controller="service" asp-action="deleteindustriesserved" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                                </td>
                            </tr>
                            i++;
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


