@model ServiceOffered
@{
    ViewData["Title"] = "Create ServiceOffereds";
}

<div class="content-wrapper">
    <div class="container">
        <div class="row">
            <div class="col-lg-6">
                <div asp-validation-summary="All" class="text-danger"></div>
                <form asp-area="admin" asp-controller="service" asp-action="createserviceoffered" method="post">
                    <div class="form-group">
                        <label asp-for="Name">Name</label>
                        <input class="form-control" id="Title" asp-for="Name">
                        <span id="messages" asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <button type="submit" class="btn btn-primary">Add</button>
                    <button asp-action="serviceoffered" class="btn btn-danger">Cancel</button>
                </form>
            </div>
        </div>
    </div>
</div>